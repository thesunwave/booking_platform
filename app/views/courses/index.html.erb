<h1>Список достпных курсов</h1>

<table>
  <thead>
      <tr>
      <td>Название(<%= link_to 'asc', courses_path(sort_by: 'name_asc') %>, <%= link_to 'desc', courses_path(sort_by: 'name_desc') %>)</td>
      <td>Описание</td>
      <td>Дата начала(<%= link_to 'asc', courses_path(sort_by: 'groups_asc') %>, <%= link_to 'desc', courses_path(sort_by: 'groups_desc') %>)</td>
      <td>Количество учеников</td>
    </tr>
  </thead>
  <tbody>
    <% courses.each do |course| %>
      <tr>
        <td><%= link_to course.name, course %></td>
        <td><%= course.description %></td>
        <td><%= course.next_group %></td>
        <td><%= course.users_count %></td>
      </tr>
    <% end %>
  </tbody>
</table>
